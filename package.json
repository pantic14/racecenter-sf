{
  "name": "racecenter-peloton",
  "private": true,
  "version": "0.1.5",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "fixtures": "node fixtures/generate-synthetic.mjs && node fixtures/generate-drop.mjs && node fixtures/generate-profile.mjs && node fixtures/make-recording.mjs synthetic-basic && node fixtures/make-recording.mjs synthetic-drop",
    "icons": "node tools/generate-icons.mjs",
    "zip": "npm run build && python3 -c \"import shutil; shutil.rmtree('dist/fixtures', ignore_errors=True); shutil.make_archive('racecenter-peloton', 'zip', 'dist')\""
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.2.0",
    "svelte": "^5.56.4",
    "vite": "^8.1.3"
  }
}
